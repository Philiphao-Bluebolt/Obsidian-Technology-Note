+ **参考**：[GeeksforGeeks](https://www.geeksforgeeks.org/dsa-tutorial-learn-data-structures-and-algorithms/)、[Wikipedia](https://en.wikipedia.org/wiki/Data_structure)

**数据结构与算法**（DSA, Data Structure and Algorithm）研究计算机储存数据的理论结构与操作形式，常见的数据结构包括数组、链表、栈、队列、树、图等，常见的数据操作包括插入、删除、排序、搜索、遍历、复制、清空等。

+ 🪈 **[线性结构 Linear Structure](#线性结构%20Linear%20Structure)**
	+ **抽象** - [序列](序列%20Sequence.md) · 栈 · 队列 · 两端队列 · 优先队列
	+ **实现** - [静态](序列%20Sequence.md#静态数组%20Static%20Array) · [动态](序列%20Sequence.md#动态数组%20Dynamic%20Array) · [链表](链表%20Linked%20List.md) · 平衡二叉树 · 环形
	+ **操作** - 搜索 ( [顺序](#顺序搜索%20Sequential%20Search) · [二分](#二分搜索%20Binary%20Search) · 双指针 ) · 排序 ( 选择 · 冒泡 · 插入 · 快速 · 堆 · 希尔 · 归并 · 桶 · 梳 ) 
+ 👉️ **[映射结构 Mapping Structure](#映射结构%20Mapping%20Structure)**
	+ **抽象** - 无序映射 · 有序映射 · 多重映射
	+ **实现** - [哈希表](映射%20Map.md#哈希表%20Hash%20Table) · 平衡二叉树 
	+ **操作** - 插入 · 删除
+ 🔆 **[分支结构 Branch Structure](#分支结构%20Branch%20Structure)**
	+ **抽象** - [树](树%20Tree.md) ( 二叉树 · 堆 ) · [图](图%20Graph.md) ( 有向无环 )
	+ **实现** - 链 · 表格 · 序列
	+ **操作** - 搜索 ( DFS · BFS ) · 寻路 ( Dijkstra · Astar )

**数据结构**分为**抽象数据结构**（ADT）与**具体数据结构**（CDT）——ADT是数学上规定了储存结构与操作的黑箱模型；CDT则是ADT在内存层面的实现，属于白箱模型。CDT常被封装成可以复用的**容器**（Container），容器内部使用的抽象数据结构可能与容器代表的数据结构本身不同，比如栈的容器可能是用数组实现的。

**操作算法**是对数据结构元素的处理方法，分为**读**、**写**两大类——读操作不改变元素，如遍历、搜索、计数、寻路；写操作则会改变元素顺序或具体数据，如插入、删除、排序、清空。

+ **插入 Insert** - (序列、树、图) 指定位置插入元素；(栈和队列) 压入元素；(映射) 新增键值对
+ **删除 Delete** - (序列、树、图) 删除指定元素或键；(栈和队列) 弹出元素；(映射) 删除指定键值对
+ **清空 Clear** - 删除所有元素
+ **复制 Copy** - 将所有元素复制到另一个容器
+ **遍历 Traverse** - 按一定顺序穷举所有元素，即线性化，本质上和搜索算法相同
+ **搜索 Search** - 寻找指定元素
+ **计数 Count** - 计算指定元素的出现次数
+ **寻路 Path** - 寻找两个元素之间的路径

实际执行时，部分操作需要先执行其他操作才能执行，如二分查找要求先对数组进行排序。

---
## 线性结构 Linear Structure

线性结构包括序列、栈、队列、两端队列、优先队列，这一类数据结构中的元素依次排列成序列，元素之间有明确的先后顺序。线性结构可以通过递归储存创建二维网格和更高维的张量

> **抽象** 

+ [序列](序列%20Sequence.md) - 可以访问或增删任意位置的元素，实现方法——静态数组、动态数组、链表
+ 栈 - 先入后出（FILO），只能访问或增删栈顶元素
+ 队列 - 先入先出（FIFO），只能访问或增删队尾元素
+ 两端队列 - 可同时访问或增删队首与队尾的元素
+ 优先队列 - 

> **实现**

+ 静态 - 
+ 动态 - 
+ 链表 - 
+ 环形 - 

---
## 映射结构 Mapping Structure

映射结构指的是同时储存键值对，可以通过指定键快速搜索元素值的数据结构，常有无序、有序、重复三种变体。



---
## 分支结构 Branch Structure




---
## 栈 Stack

+ **参考**：

栈是一种遵循**先入后出**（FILO）原则的线性数据结构，其储存原理类似盒子或者箱子：最先放入的元素被压在栈的底端，而最后放入的元素位于栈的顶端，只有栈的顶端元素才能被移除。栈的基本操作包括压入元素、弹出顶端元素、访问指定元素值。

+ 栈的容器实现
	+ C++：由标准库`<stack>`引入的`std::stack`
	+ Python：
	+ Java：


---
## 队列 Queue






---
## 选择排序 Selection Sorting

+ **参考**：





---
## 冒泡排序 Bubbleing Sorting


---
## 插入排序 Insertion Sorting


---
## 合并排序 Merge Sorting


---
## 快速排序 Quick Sorting



---
## 堆排序 Heap Sort

+ **参考**：[Geeksforgeeks](https://www.geeksforgeeks.org/heap-sort/)
+ **类型**：
+ **复杂度**：时间 $O()$ - 空间 $O()$


---
## 顺序搜索 Sequential Search

+ **复杂度**：时间 $O(n)$ - 空间 $O(1)$

顺序搜索是针对线性容器最简单的搜索方法，即从第一个元素搜索到最后一个元素，最坏情况下的时间复杂度为线性复杂度$O(n)$

### 实现代码

> **C++**

```C++
int seqSearch(vector<int>& nums, int target) {
	for (int i = 0; i < nums.size(); i++) {
		if (nums[i] == target) return i;
	}
	return -1;
}
```

---
## 二分搜索 Binary Search

+ **参考**：[Geeksforgeeks](https://www.geeksforgeeks.org/binary-search/)
+ **复杂度**：时间 $O(\log n)$ - 空间 $O(1)$

二分搜索又称为折半搜索，其基本思想是：在一个**已排序**的序列中，每次只需检测正中间的元素便可将查找范围缩小一半，从而将时间复杂度降低到对数级别，是线性结构查找算法中效率最高的算法。

每一轮迭代的中位元素序号`mid`可以由子序列的最左侧元素序号`low`和最右侧元素序号`high`取平均得到。子序列长度为奇数时`mid`指向中间元素，为偶数时指向中间靠左元素（整型去尾）。

```C++
mid = (low + high) / 2

// oddArr = [1, 2, 3], mid = 2
// evenArr = [1, 2, 3, 4], mid = 2
```

继续迭代的条件需要取等号，否则在两边的指针重叠时会忽略指向值

```C++
while (low <= high)
```

### 实现代码

> **C++**

```C++
int binarySearch(vector<int>& nums, int target) {

    int left = 0, right = nums.size() - 1, mid = 0;
    while (left <= right) {
        mid = (left + right) / 2;
        if (target < nums[mid]) right = mid - 1;
        else if (target > nums[mid]) left = mid + 1;
        else return mid;
    }
	return -1;
}
```

> **Java**

```java
int binarySearch(int arr[], int x) 
    {
        int low = 0, high = arr.length - 1;
        while (low <= high) {
            int mid = low + (high - low) / 2;

            if (arr[mid] == x) // Check if x is at mid
                return mid;

            if (arr[mid] < x) // If x greater, ignore left half
                low = mid + 1;

            else // If x is smaller, ignore right half
                high = mid - 1;
        }

        // If we reach here, then element was not present
        return -1;
    }
```


---
## 双指针搜索 Two Pointer Search

+ **参考**：[Geeksforgeeks](https://www.geeksforgeeks.org/two-pointers-technique/)
+ **复杂度**：时间 $O(n)$ - 空间 $O(1)$

双指针搜索是一种在已排序的线性容器中搜寻元素对的方法，可以将搜索两个元素的时间复杂度降低到线性级别$O(n)$，其基本思想是在遍历过程中跳过的元素对。

---
## 深度优先搜索 Depth-first Search




---
## 广度优先搜索 Breadth-first Search