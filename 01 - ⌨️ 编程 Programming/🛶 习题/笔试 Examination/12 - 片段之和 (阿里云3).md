给定一个数组`arr`，同时定义运算`g(x)`表示数组内所有长度为`x`的子区间元素之和。求数组`arr`在区间`[l, h]`内所有质数元素`p`对应的`g(p)`之和，将结果输出为除214748364的余数以避免溢出。

输入格式如下，第一行两个数字分别表示数组长度`n`和测试组数`m`，第二行表示数组元素，其余各行两个数字表示每个测试的区间下界`l`和上界`h`。

```
7 3
7 4 2 5 5 1 2
2 6
0 4
```