一个九个字符的验证码由八个编码字符和一个纠错字符组成。编码字符的前两位是字母，后六位是数字。纠错字符的生成规则是，前八个字符的ASCII码相加后对26取余，然后加上字母A的ASCII码（一种哈希函数）

现在输入一个验证码

```
BF539263
```

+ 若验证码正确，直接输出原验证码
+ 若验证码前八个字符的格式正确，但纠错字符有误，输出改正纠错字符后的验证码
+ 若验证码前八个字符的格式错误，输出字符串“Invalid”

---
## 思路

这题很简单，验证码出错的可能性包括：

+ 纠错字符有误
+ 验证码前八个字符的格式出错
+ 验证码字符缺失或过多

---
## 