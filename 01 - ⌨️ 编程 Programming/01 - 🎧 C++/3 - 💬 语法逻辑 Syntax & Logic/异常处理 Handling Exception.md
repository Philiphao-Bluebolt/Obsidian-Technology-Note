+ **关键词**：`try`、`catch`、`throw`

异常处理是用于处理运行异常（Runtime Error）的机制，结合判断语句使用。


---
## Throw

```c++
int x > 0;
cin >> x;
if (x < 0) throw std::runtime_error("x should be positive!");
```

```C++
try {
	if (x < 0) 
	throw str(x);
}


```