给定一个字符串，里面包含重复的“xdp”组合结构，其中“x”是一个不含字母“d”和“p”字符串。统计字符串里一共有多少种“xdp”结构，尝试写出时间复杂度为$O(n)$的算法

输入格式

```
hadptdpttdpyudptdp
```

---
## 思路

要确定一种“xdp”结构有无出现过，必须将所有出现过的组合储存起来，再进行比较。为了实现线性的时间复杂度，储存的数据结构应当使用哈希表。

---
## 